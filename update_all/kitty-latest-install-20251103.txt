# from: https://sw.kovidgoyal.net/kitty/binary/
# install latest kitty to enable text blinking ...
# ---
curl -L https://sw.kovidgoyal.net/kitty/installer.sh | sh /dev/stdin
sudo mv /usr/bin/kitty{,.old}
sudo ln -s ~/.local/kitty.app/bin/kitty /usr/bin/kitty
sudo ln -s ~/.local/kitty.app/bin/kitten /usr/bin/kitten
# ---

