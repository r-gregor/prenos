#! /usr/bin/env bash
# fname: string-upper-to-lower-en
# 20260331
# last: 20260331
# ---

arg="$@"

SMALL=$(echo "$arg" | sed 's/ \././' | tr [:upper:] [:lower:])

echo "$SMALL"
echo "$SMALL" | putclip   # use xclip on Linux!!

